# 阶段性交接文档 — 坦克对战demo v0.16.1

> 生成时间：2026-05-06 16:27
> 给接手的有多模态能力的模型

---

## 一、项目概览

**项目**：浏览器端 3D 坦克对战游戏
**技术栈**：Three.js 0.160（本地 `three.min.js`）+ 原生 JS + Web Audio API
**运行方式**：双击 `index.html` 即可运行（`file://` 协议），无需服务器
**代码仓库**：
- Gitee：`git@gitee.com:hpmax9696/tank_demo.git`
- GitHub：`git@github.com:hpmax9696/tank_demo.git`

---

## 二、项目结构

```
c:\Users\hpmax\CodeBuddy\20260506080419\
├── index.html              # 主游戏文件（HTML+CSS+JS, ~1970行）
├── three.min.js            # Three.js 本地库
├── fireSmokeParticles.js   # 火焰/烟雾/爆炸粒子系统
├── README.md               # 项目文档
└── models/
    ├── modelRegistry.js    # 模型注册表（注册/获取/预览/权重随机）
    ├── tank.js             # 原版坦克模型（游戏中使用）
    ├── t34-85.js           # T-34-85后期型模型（仅模型库预览）
    ├── trees.js            # 树木模型
    ├── buildings.js        # 建筑模型
    └── windmill.js         # 风车模型
```

**OneDrive 备份目录**（实际测试用）：
`C:\Users\hpmax\OneDrive\共享软件\坦克对战demo\`

---

## 三、本次会话完成的工作

### 3.1 创建 T-34-85 后期型模型（v0.16.0 → v0.16.1）

在 `models/t34-85.js` 中创建了一个详细的 T-34-85 (Model 1944/1945) 坦克 3D 模型，已注册到模型注册表：
- `t34-85-green`（绿色涂装）
- `t34-85-desert`（沙漠涂装）

可通过主菜单 **「🔍 模型预览」** 查看。

### 3.2 v0.16.1 修正（根据用户反馈）

| # | 问题 | 修正方案 |
|---|------|---------|
| 1 | 车头是垂直盒面 | 改用 `THREE.ExtrudeGeometry` 挤出通体楔形剖面，60°首上装甲为一条连续斜线 |
| 2 | 炮塔是完美半球 | 改为截顶圆锥厚壁(高0.24) + 浅穹顶上收；炮塔底径1.06 > 车宽0.94 |
| 3 | 炮管短粗，无防盾 | 炮管加长至1.15(≈车体2/3)、半径收至0.033；添加突出矩形防盾(0.32×0.24×0.12) |
| 4 | 履带太薄，主动轮无特征 | 履带片加厚至0.07×0.035；主动轮r=0.18,y=0.22(负重轮r=0.14,y=0.14)，带14齿 |

### 3.3 模型技术参数（当前 t34-85.js）

| 部件 | 关键参数 |
|------|---------|
| 车体剖面 | Shape: (0.85,0.25)→(0.33,0.55)→(-0.85,0.55)→(-0.85,0.25), 挤出深度0.94 |
| 首上倾角 | 60°从垂直面（30°从水平面） |
| 炮塔下部 | Cylinder(topR=0.50, bottomR=0.53, h=0.24, segs=36) |
| 炮塔穹顶 | Sphere(r=0.53, theta=0~0.42π) |
| 矩形防盾 | Box(0.32, 0.24, 0.12), pos z=TURRET_R-0.04 |
| 主炮管 | Cylinder(r=0.033~0.035, len=1.15), 中段抽烟器r=0.051 |
| 负重轮×5 | r=0.14, y=0.14, z=-0.40,-0.20,0,0.20,0.40 |
| 诱导轮 | r=0.10, y=0.12, z=0.75（前下方） |
| 主动轮 | r=0.18, y=0.22, z=-0.68（后上方），14齿 |
| 履带 | 底部y=0.01水平 + 顶部y=0.27水平 + 前后6段弧形 |

### 3.4 游戏接口兼容性

模型为 `group.userData` 设置了以下字段（游戏代码依赖）：
- `hull` — 车体 Mesh 引用
- `leftWheels` — 左侧轮组 Group 数组（游戏通过 `w.rotation.x` 驱动旋转）
- `rightWheels` — 右侧轮组 Group 数组

**重要**：单人模式和对战模式仍使用 `models/tank.js` 中的原版坦克，T-34-85 仅在模型预览中可用。

---

## 四、游戏主要系统

| 系统 | 关键参数 |
|------|---------|
| 世界 | 200×200，350个泊松盘障碍物，中心10单位安全区 |
| 坦克物理 | 履带间距3.2，最高速度4.0，差速驱动 |
| 火炮 | 初速22，低重力1.0平射，装填2秒，伤害20 |
| 分屏 | 双人对战左右分屏，距>25显示指向箭头 |
| 粒子 | DamageEffects(持续火焰烟雾HP<50) + ExplosionEffects(死亡爆炸) |
| 殉爆 | 坦克死亡爆炸3.5米内障碍物连锁引爆 |

---

## 五、当前已知问题

1. **T-34-85 模型未经过视觉验证**：用户无法准确描述外观，不确定模型是否与真实 T-34-85 后期型一致
2. 用户提到"轮子沉到地面下方"和"履带只有上面一条"——已在 v0.16.1 中修正（车轮中心Y=半径，履带完整环路）
3. 模型预览中，炮塔和炮管是固定在 `turretGroup`/`barrelGroup` 中的，预览时不旋转。游戏中使用时炮塔跟随车体朝向（因当前游戏未实现独立炮塔旋转）

---

## 六、需要多模态能力接手的工作

### 6.1 视觉验证 T-34-85 模型

请打开 `http://localhost:8123/index.html`（或双击 OneDrive 中的 index.html），进入模型预览，对照参考图验证以下特征：

1. **首上装甲**：从侧面看，车体前部是否呈现一条清晰的60°斜线？
2. **炮塔形状**：是否像"截顶圆锥+圆顶"而非完美半球？炮塔是否比车体宽？
3. **炮管**：是否细长（长度≈车体2/3）？防盾是否可见？
4. **主动轮**：后上方的轮子是否明显比负重轮大且带齿？
5. **履带**：是否形成完整环状包裹车轮？

### 6.2 潜在的模型修正方向

如果视觉效果不对，修改 `models/t34-85.js` 中的以下常量即可快速调整：

```javascript
const HULL_W = 0.94;          // 车体宽度
const GLACIS_DZ = 0.52;      // 首上水平跨度（控制倾角）
const WL_R = 0.14;           // 负重轮半径
const SPROCKET_R = 0.18;     // 主动轮半径（加大则更明显）
const SPROCKET_Y = 0.22;     // 主动轮高度（加高则更高）
const TURRET_OUTER_R = 0.53; // 炮塔外径（加大则更宽）
const mainBarrelLen = 1.15;  // 炮管长度
```

### 6.3 可能的后续任务

- 将 T-34-85 模型替换到单人/对战模式中
- 实现炮塔独立旋转（当前游戏未实现）
- 添加履带纹理/细节
- 增加更多涂装变体

---

## 七、更新策略（每次代码修改后执行）

```
git add -A
git commit -m "vX.Y.Z: 描述"
git push origin master          # Gitee
git push github master          # GitHub (30秒超时，失败跳过)
xcopy index.html + fireSmokeParticles.js + README.md + models/* → OneDrive目录
```
